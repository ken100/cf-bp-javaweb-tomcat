#!/usr/bin/env ruby
 
# sync output
$stdout.sync = true
$stderr.sync = true
 
build_dir = ARGV[0]
if Dir.exists?("#{build_dir}/WEB-INF") and File.exists?("#{build_dir}/WEB-INF/web.xml")
  puts 'Ok, it is a java web application.'
  exit 0
end
 
puts 'Sorry, it is not a java web application.'
exit 1
