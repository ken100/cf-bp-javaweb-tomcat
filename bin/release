#!/usr/bin/env ruby

require 'yaml'

yml = {
    'addons' => [],
    'config_vars' => {},
    'default_process_types' => {
        'web' => './apache-tomcat-7.0.54/bin/catalina.sh run'
    }
}.to_yaml

puts yml

